
<main class="main">


<% @pictures.each do |picture| %>
  <figure class="caption1"><figcap class="caption"><%= picture.title %>, <%= picture.artist %></figcap></figure>
  <figure class="mainpics"><%= link_to image_tag(picture.url), "pictures/#{picture.id}" %></figure>
<% end %>
</main>

