
<main class="main">


<% @pictures.each do |picture| %>
  <figure><figcap class="caption"><%= picture.title %>, <%= picture.artist %></figcap></figure>
  <%= link_to image_tag(picture.url), "pictures/#{picture.id}" %>
<% end %>
</main>

